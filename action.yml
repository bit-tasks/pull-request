name: 'Bit Pull Request'
description: 'Build components, snap and update lane for Pull Requests'
branding: 
  icon: 'git-pull-request'
  color: 'purple'
inputs:
  ws-dir:
    description: "Workspace json file directory path"
    required: false
  debug:
    description: "Debug bit commands"
    required: false
runs:
  using: 'node20'
  main: 'dist/index.js'
